export type Position = {
    x: number;
    y: number;
    z: number;
}

export enum Side {
    FRONT = 'front',
    BACK = 'back',
    LEFT = 'left',
    RIGHT = 'right',
    TOP = 'top',
    BOTTOM = 'bottom',
    CENTER = 'center'
}